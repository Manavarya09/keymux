[package]
name = "keymux-desktop"
version = "0.1.0"
edition = "2021"

[dependencies]
tauri = { version = "2.5.0", features = ["api-all"] }
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serial_test = "0.5"
