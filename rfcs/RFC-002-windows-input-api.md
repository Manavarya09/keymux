# RFC: Windows Input API (Tauri bindings)

Defines the Tauri command surface: `list_devices()` and `start_listening()` returning JSON-serializable structures. Commands follow the cross-platform input module abstractions.
